import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'prediccion.ventas',
  appName: 'predictionhouse',
  dist: 'www',
  bundledWebRuntime: false
};

export default config;
